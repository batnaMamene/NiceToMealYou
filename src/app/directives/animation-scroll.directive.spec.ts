import { AnimationScrollDirective } from './animation-scroll.directive';

describe('AnimationScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationScrollDirective();
    expect(directive).toBeTruthy();
  });
});
